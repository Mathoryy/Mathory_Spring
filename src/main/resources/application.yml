server:
  port: 7007

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mathory_db
    username: mathory
    password: mathory123@!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      connection-timeout: 20000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
